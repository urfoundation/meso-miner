name: functional-soak

on:
  # Manual-only: a 3h test should not auto-fire on every push (a push +
  # manual trigger doubles it). Dispatch deliberately when a soak is wanted.
  workflow_dispatch:

jobs:
  native-soak:
    name: Linux functional soak (accelerated, ~3h)
    runs-on: ubuntu-latest
    timeout-minutes: 200  # 3h20m; job cap is 360
    env:
      GAUNTLET_USER: ${{ secrets.GAUNTLET_USER }}
      GAUNTLET_PW: ${{ secrets.GAUNTLET_PW }}
      # Accelerated schedule: compress the pool-stabilization timeline so
      # proxies warm up in ~1-2h instead of 6h+. These are TEST-ONLY knobs;
      # they are not the production defaults.
      URNETWORK_PROXY_BENCHMARK: "true"
      URNETWORK_SELF_HEAL: "1"
      PROXY_URL_REFRESH: "2m"
      URNETWORK_HEARTBEAT_INTERVAL: "15s"
      URNETWORK_REPORT_INTERVAL: "1m"
      # Tight cap on URL-sourced proxies: keeps the runner's external-IP
      # footprint small and contained (ToS-conscious), while still loading
      # enough of a real pool to observe admission/grading/stabilization.
      PROXY_URL_MAX: "25"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.27"

      - name: Build provider + tool
        run: |
          go build -o /tmp/provider ./provider/
          go build -o /tmp/urnet-tools ./cmd/urnet-tools/

      - name: Mint a real JWT (gauntlet account)
        run: |
          mkdir -p /tmp/mint
          HOME=/tmp/mint /tmp/provider auth \
            --user_auth="$GAUNTLET_USER" --password="$GAUNTLET_PW" \
            --api_url=https://api.bringyour.com -f 2>&1 | grep -vE '^I[0-9]{4}'
          test -s /tmp/mint/.urnetwork/jwt || { echo "JWT mint failed — see auth output above"; exit 1; }
          if grep -qE '"(network_id|name)"\s*:\s*"(production|full-bars)"' /tmp/mint/.urnetwork/network.json 2>/dev/null; then
            echo "REFUSING: production account"; exit 1
          fi
          echo "JWT minted OK"

      - name: Set up state dir + accelerated grade config + pre-write source
        run: |
          mkdir -p "$HOME/.urnetwork"
          cp /tmp/mint/.urnetwork/jwt "$HOME/.urnetwork/jwt"
          # Compress the stage-1 probe cadence (test-only, re-read live).
          cat > "$HOME/.urnetwork/proxy_grades.json" << 'EOF'
          {
            "interval_sec": 60,
            "sample_width": 6,
            "pass_bar": 0.5,
            "stale_calm": "30m",
            "stale_hot": "15m"
          }
          EOF
          # Pre-write the URL source into the state file BEFORE the provider
          # starts. provide() merges persisted Sources (resolveProxyURLs), so
          # the background fetcher loads under --proxy_url_max=25 from the
          # very first cycle — no add-source one-shot (maxTotal=0) ever runs,
          # so the cap cannot be bypassed. This is also the real operator
          # flow: configure the source once, provider picks it up on every
          # restart.
          cat > "$HOME/.urnetwork/proxy_url.json" << 'EOF'
          {
            "sources": ["https://raw.githubusercontent.com/monosans/proxy-list/main/proxies/socks5.txt"],
            "cache": {}
          }
          EOF
          echo "state + accelerated config + pre-written source ready"

      - name: Start provider (real session, accelerated, capped)
        run: |
          # No --proxy_url flag needed: the persisted source in
          # proxy_url.json is merged by resolveProxyURLs. The cap is set via
          # env so the running fetcher honors it from the first cycle.
          nohup /tmp/provider provide > /tmp/provider.log 2>&1 &
          echo $! > /tmp/provider.pid
          sleep 30
          if kill -0 $(cat /tmp/provider.pid) 2>/dev/null; then
            echo "provider running (pid $(cat /tmp/provider.pid))"
          else
            tail -30 /tmp/provider.log; exit 1
          fi

      - name: Basic tool commands
        run: |
          /tmp/urnet-tools providers
          /tmp/urnet-tools status
          /tmp/urnet-tools version

      # ---- Soak block 1: 50 min pool observation ----
      - name: Soak block 1 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block1 window $i @ $(date -u +%T) ---"
            /tmp/urnet-tools proxy health 2>/dev/null | head -5 || true
            /tmp/urnet-tools providers
            sleep 300
          done

      - name: Decode client_id helper
        run: |
          cat > /tmp/jwt-client-id.sh << 'EOF'
          #!/bin/bash
          # Decode client_id from a raw JWT (payload = 2nd dot-segment,
          # base64url). client_id is a claim, never plaintext in the file.
          f="$1"
          [ -s "$f" ] || { echo ""; exit 0; }
          payload=$(cut -d. -f2 "$f")
          # base64url -> base64
          b64=$(echo "$payload" | tr '_-' '/+')
          # pad
          case $((${#b64} % 4)) in
            2) b64="${b64}==" ;;
            3) b64="${b64}=" ;;
          esac
          echo "$b64" | base64 -d 2>/dev/null | grep -oE '"client_id"\s*:\s*"[^"]*"' | head -1 | cut -d'"' -f4
          EOF
          chmod +x /tmp/jwt-client-id.sh
          echo "helper ready"

      - name: Restart cycle 1 (client-ID reuse check)
        run: |
          echo "client_id before: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          /tmp/urnet-tools hot-restart -f || echo "hot-restart nonzero"
          sleep 30
          echo "client_id after: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          /tmp/urnet-tools providers

      # ---- Soak block 2: 50 min pool observation ----
      - name: Soak block 2 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block2 window $i @ $(date -u +%T) ---"
            /tmp/urnet-tools proxy health 2>/dev/null | head -5 || true
            /tmp/urnet-tools providers
            sleep 300
          done

      - name: Restart cycle 2 (client-ID reuse check)
        run: |
          echo "client_id before: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          /tmp/urnet-tools hot-restart -f || echo "hot-restart nonzero"
          sleep 30
          echo "client_id after: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          /tmp/urnet-tools providers

      # ---- Soak block 3: 50 min pool observation ----
      - name: Soak block 3 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block3 window $i @ $(date -u +%T) ---"
            /tmp/urnet-tools proxy health 2>/dev/null | head -5 || true
            /tmp/urnet-tools providers
            sleep 300
          done

      - name: Remint cycle (new client ID check)
        run: |
          echo "client_id before: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          rm -f "$HOME/.urnetwork/jwt"
          rm -rf /tmp/remint && mkdir -p /tmp/remint
          HOME=/tmp/remint /tmp/provider auth \
            --user_auth="$GAUNTLET_USER" --password="$GAUNTLET_PW" \
            --api_url=https://api.bringyour.com -f 2>&1 | grep -vE '^I[0-9]{4}' || true
          cp /tmp/remint/.urnetwork/jwt "$HOME/.urnetwork/jwt" 2>/dev/null || true
          sleep 10
          echo "client_id after remint: $(/tmp/jwt-client-id.sh "$HOME/.urnetwork/jwt")"
          /tmp/urnet-tools providers

      - name: proxy clear + refresh (wrapper-fix parity w/ docker soak) + final restart
        run: |
          /tmp/urnet-tools proxy clear -f || echo "clear nonzero"
          # --force bypasses refresh's warmup gate, mirroring the docker
          # soak's wrapper-fix check on the same command surface.
          /tmp/urnet-tools proxy refresh --force || echo "refresh nonzero"
          sleep 30
          /tmp/urnet-tools providers
          /tmp/urnet-tools hot-restart -f || echo "hot-restart nonzero"
          sleep 20
          /tmp/urnet-tools providers

      - name: Teardown
        run: |
          # Full teardown: SIGTERM lets the provider deregister, then the
          # state dir is wiped so the client JWT cannot resurrect the same
          # client_id on a later run. Runs even when the job fails.
          kill -TERM $(cat /tmp/provider.pid) 2>/dev/null || true
          sleep 3
          rm -rf "$HOME/.urnetwork" /tmp/mint 2>/dev/null || true
          echo "--- provider log tail ---"
          tail -30 /tmp/provider.log 2>/dev/null || true
          echo "--- provider log: errors during soak ---"
          grep -iE "error|panic|fatal|oom" /tmp/provider.log 2>/dev/null | tail -10 || echo "no errors logged"
        if: always()

  docker-soak:
    name: Docker provider soak (accelerated, ~3h)
    runs-on: ubuntu-latest
    timeout-minutes: 200
    env:
      GAUNTLET_USER: ${{ secrets.GAUNTLET_USER }}
      GAUNTLET_PW: ${{ secrets.GAUNTLET_PW }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.27"

      - name: Build urnet-docker
        run: go build -o /tmp/urnet-docker ./cmd/urnet-docker/

      - name: Build the provider image locally (includes the wrapper fix)
        # Pulling :latest would test the STALE published image (old wrapper
        # without the clear fix). Build from the checkout so the soak tests
        # the current code, mirroring the binary soak.
        run: |
          docker build -t urnetwork-test:local .
          echo "local image built"

      - name: Start provider container (BUILD=stable, capped at 25)
        run: |
          cat > /tmp/docker-compose.yml << 'EOF'
          services:
            urfix-soak:
              image: urnetwork-test:local
              container_name: urfix-soak
              restart: unless-stopped
              cap_add: [NET_ADMIN, NET_RAW]
              sysctls:
                net.ipv4.ip_forward: 1
              environment:
                - BUILD=stable
                - USER_AUTH=${{ secrets.GAUNTLET_USER }}
                - PASSWORD=${{ secrets.GAUNTLET_PW }}
                - URNETWORK_NODE_NAME=urfix-soak
                - URNETWORK_PROFILE=auto
                # Cap URL-sourced proxies at 25 (ToS-conscious, small footprint).
                - PROXY_URL_MAX=25
              volumes:
                - ur_config:/root/.urnetwork
          volumes:
            ur_config:
          EOF
          cd /tmp && docker compose -f docker-compose.yml up -d
          echo "container started; waiting for auth..."
          sleep 90
          docker ps --filter name=urfix-soak --format "{{.Names}} {{.Status}}"

      - name: Verify provider authenticated (fail if not)
        run: |
          if ! docker exec urfix-soak test -s /root/.urnetwork/jwt 2>/dev/null; then
            echo "FAIL: no JWT in container — provider did not authenticate"
            docker logs urfix-soak 2>&1 | tail -20
            exit 1
          fi
          echo "OK: provider authenticated"

      - name: Add URL source via urnet-docker (host-side, Design 2)
        run: |
          /tmp/urnet-docker proxy add-source --unit urfix-soak \
            "https://raw.githubusercontent.com/monosans/proxy-list/main/proxies/socks5.txt"

      - name: Basic tool commands
        run: |
          /tmp/urnet-docker providers
          /tmp/urnet-docker status urfix-soak | head -8

      - name: Soak block 1 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block1 window $i @ $(date -u +%T) ---"
            /tmp/urnet-docker exec --unit urfix-soak -- urnet-tools proxy summary 2>/dev/null | head -5 || true
            /tmp/urnet-docker providers
            sleep 300
          done

      - name: proxy clear (tests the wrapper fix) + refresh
        run: |
          /tmp/urnet-docker proxy clear --unit urfix-soak
          # --force bypasses the 8-12h warmup gate (refresh's usage has
          # [--force]; the gate lives in proxyRefresh). Without it a fresh
          # provider exits 52.
          /tmp/urnet-docker proxy refresh --force --unit urfix-soak
          sleep 30
          /tmp/urnet-docker providers

      - name: Soak block 2 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block2 window $i @ $(date -u +%T) ---"
            /tmp/urnet-docker exec --unit urfix-soak -- urnet-tools proxy summary 2>/dev/null | head -5 || true
            /tmp/urnet-docker providers
            sleep 300
          done

      - name: Container restart (client-ID reuse check)
        run: |
          /tmp/urnet-docker restart --force urfix-soak
          sleep 30
          /tmp/urnet-docker providers

      - name: Soak block 3 (50 min pool observation)
        run: |
          for i in $(seq 1 10); do
            echo "--- block3 window $i @ $(date -u +%T) ---"
            /tmp/urnet-docker exec --unit urfix-soak -- urnet-tools proxy summary 2>/dev/null | head -5 || true
            /tmp/urnet-docker providers
            sleep 300
          done

      - name: Teardown
        run: |
          # Full teardown: SIGTERM (compose stop) lets the in-container
          # provider deregister, then -v removes the config volume so the
          # client JWT cannot persist. Runs even when the job fails.
          cd /tmp && docker compose -f docker-compose.yml stop 2>/dev/null || true
          docker compose -f docker-compose.yml down -v 2>/dev/null || true
          echo "--- container logs (errors) ---"
          docker logs urfix-soak 2>&1 | grep -iE "error|panic|fatal" | tail -10 || echo "no errors"
        if: always()
